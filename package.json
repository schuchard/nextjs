{
  "name": "next-test",
  "version": "1.0.0",
  "main": "index.js",
  "private": true,
  "scripts": {
    "dev": "next",
    "build": "next build",
    "start": "yarn clean:out && next export && cd out && serve -p 8000",
    "start:build": "next build && yarn start",
    "start:server": "next start",
    "clean:out": "rm -rf ./out"
  },
  "dependencies": {
    "bulma": "^0.6.2",
    "next": "^5.1.0",
    "react": "^16.3.1",
    "react-dom": "^16.3.1"
  },
  "devDependencies": {
    "serve": "^6.5.3"
  }
}
